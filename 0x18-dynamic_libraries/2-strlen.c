#include "main.h"
#include <string.h>

/**
 * _strlen -  Returns the length of a string.
 *@s: The string it returns a length of.
 *Return: The length of string.
 */
int _strlen(char *s)
{
	return ((int)strlen(s));
}
