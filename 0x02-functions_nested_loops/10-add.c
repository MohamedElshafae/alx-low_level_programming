#include "main.h"

/**
 * add - Take 2 numbers add them and return the putput.
 * @x: The first number.
 * @y: The second number.
 * Return: The sum of them.
 */
int add(int x, int y)
{
	return (x + y);
}
